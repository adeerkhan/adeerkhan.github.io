"use client";

import { addCollection } from "@iconify/react";
import { icons as mdi } from "@iconify-json/mdi";
import { icons as logos } from "@iconify-json/logos";
import { icons as si } from "@iconify-json/simple-icons";
import { icons as ph } from "@iconify-json/ph";
import { icons as devicon } from "@iconify-json/devicon";
import { icons as thesvg } from "@iconify-json/thesvg";

addCollection(mdi);
addCollection(logos);
addCollection(si);
addCollection(ph);
addCollection(devicon);
addCollection(thesvg);

export function IconRegistry() {
  return null;
}
