# Agent Instructions

- After completing any code changes, commit and push to `main` (CI deploys automatically).
- Verify with `npm run lint` and `npm run test` (and `npm run build` for bigger changes) before pushing.
